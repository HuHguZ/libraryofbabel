# A page address is ~6,000 characters and a found phrase rides along in `?q=`; Node's default
# 16 KB limit on request headers (the URL counts) turns long searches into "431". npm scripts pass this on.
node-options=--max-http-header-size=262144
